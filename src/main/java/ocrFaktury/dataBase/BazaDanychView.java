package ocrFaktury.dataBase;import javafx.geometry.Insets;import javafx.scene.Scene;import javafx.scene.control.*;import javafx.scene.control.cell.PropertyValueFactory;import javafx.scene.layout.*;import javafx.stage.Stage;import java.util.List;public class BazaDanychView {    private TableView<FakturaRecord> table;    private FakturaDAO dao = new FakturaDAO();    public void show() {        Stage stage = new Stage();        stage.setTitle("📦 BAZA DANYCH SQL");        stage.setWidth(800);        stage.setHeight(600);        // Tabela danych        table = new TableView<>();        table.setColumnResizePolicy(TableView.CONSTRAINED_RESIZE_POLICY);        table.setStyle("-fx-font-size: 14px;");        TableColumn<FakturaRecord, Integer> idCol = new TableColumn<>("ID");        idCol.setCellValueFactory(new PropertyValueFactory<>("id"));        TableColumn<FakturaRecord, String> numerCol = new TableColumn<>("Numer Faktury");        numerCol.setCellValueFactory(new PropertyValueFactory<>("numerFaktury"));        TableColumn<FakturaRecord, String> dataCol = new TableColumn<>("Data");        dataCol.setCellValueFactory(new PropertyValueFactory<>("data"));        table.getColumns().addAll(idCol, numerCol, dataCol);        refreshTable();        // Pole SQL z placeholderem        TextField sqlField = new TextField();        sqlField.setPromptText("Wpisz zapytanie SQL");        sqlField.setPrefWidth(600);        sqlField.setStyle("-fx-font-size: 14px; -fx-prompt-text-fill: derive(-fx-control-inner-background, -30%);");        Button executeBtn = new Button("Wykonaj");        executeBtn.setStyle(                "-fx-background-color: #2c3e50; " +                        "-fx-text-fill: white; " +                        "-fx-font-weight: bold; " +                        "-fx-padding: 8 16; " +                        "-fx-background-radius: 8px;"        );        executeBtn.setOnAction(e -> executeSQL(sqlField.getText()));        HBox sqlBox = new HBox(10, sqlField, executeBtn);        sqlBox.setPadding(new Insets(10));        sqlBox.setStyle("-fx-background-color: #ecf0f1;");        HBox.setHgrow(sqlField, Priority.ALWAYS);        VBox layout = new VBox(sqlBox, table);        layout.setPadding(new Insets(10));        layout.setSpacing(10);        layout.setStyle("-fx-background-color: #fdfdfd;");        Scene scene = new Scene(layout);        stage.setScene(scene);        stage.show();    }    private void refreshTable() {        List<FakturaRecord> dane = dao.getAllFaktury();        table.getItems().setAll(dane);    }    private void executeSQL(String query) {        if (query.trim().toLowerCase().startsWith("select")) {            List<FakturaRecord> dane = dao.executeSelectQuery(query);            table.getItems().setAll(dane);        } else {            dao.executeUpdateQuery(query);            refreshTable();        }    }}